
  # haydn copia

  This is a code bundle for haydn copia. The original project is available at https://www.figma.com/design/fIhndrl6JcvcO4ffElTJ2V/haydn-copia.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  